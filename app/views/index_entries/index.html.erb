<%- model_class = IndexEntry -%>
<div class="page-header">
  <h3><%=t 'Pollen Index in Alphabetical Order' %></h3>
  <% if user_signed_in? %>
  <%= link_to t('.new_entry', :default => t("helpers.links.new_entry")),
            new_index_entry_path,
            :class => 'btn btn-default btn-xs' %>
  <% end %>
</div>
<table class="table">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:latin_name).titleize %></th>
      <th><%= model_class.human_attribute_name(:family).titleize %></th>
      <th><%= model_class.human_attribute_name(:common_name).titleize %></th>
      <% if user_signed_in? %>
        <th><%=t '.actions', :default => t("helpers.actions") %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @index_entries.each do |index_entry| %>
      <tr>
        <td><%= link_to index_entry.latin_name, pollen_images_path %></td>
        <td><%= link_to index_entry.family, pollen_images_path %></td>
        <td><%= link_to index_entry.common_name, pollen_images_path %></td>
        <% if user_signed_in? %>
          <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_index_entry_path(index_entry), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      index_entry_path(index_entry),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-default btn-xs' %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
